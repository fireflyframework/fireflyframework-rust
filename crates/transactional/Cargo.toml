[package]
name = "firefly-transactional"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Firefly Framework for Rust — with_tx transaction helper over a database port"

[dependencies]
thiserror = { workspace = true }
async-trait = { workspace = true }
# `tokio::task_local!` carries the per-transaction event buffer that backs
# `@TransactionalEventListener`; `inventory` discovers `#[event_listener]`s.
tokio = { workspace = true }
inventory = { workspace = true }

[dev-dependencies]
rusqlite = { workspace = true }
