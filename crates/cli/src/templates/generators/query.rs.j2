//! {{ names.human }} CQRS query.

use firefly_cqrs::Message;
use serde::{Deserialize, Serialize};

/// The `{{ names.pascal }}` query. Dispatch it with `bus.query(q)`; the handler
/// in `{{ names.snake }}_query_handler.rs` produces an `Option<serde_json::Value>`.
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct {{ names.pascal }} {
    /// Identifier to look up.
    pub id: String,
}

impl Message for {{ names.pascal }} {}
