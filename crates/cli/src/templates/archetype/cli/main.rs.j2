//! {{ name }} — Firefly Framework `cli` application entry point.

mod commands;
mod services;

use clap::Parser;

/// {{ name }} command-line interface.
#[derive(Debug, Parser)]
#[command(name = "{{ package_name }}", version, about = "{{ name }}")]
struct Cli {
    /// Name to greet.
    #[arg(default_value = "world")]
    name: String,
}

fn main() {
    let cli = Cli::parse();
    let service = services::greeting_service::GreetingService::new();
    println!("{}", service.greet(&cli.name));
}
