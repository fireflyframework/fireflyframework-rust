//! {{ name }} — Firefly Framework `cli` application entry point.

use clap::Parser;

use {{ package_name }}::commands::hello_command;

/// {{ name }} command-line interface.
#[derive(Debug, Parser)]
#[command(name = "{{ package_name }}", version, about = "{{ name }}")]
struct Cli {
    /// Name to greet.
    #[arg(default_value = "world")]
    name: String,
}

fn main() {
    let cli = Cli::parse();
    println!("{}", hello_command::run(&cli.name));
}
