//! {{ name }} — Firefly Framework `cli` application library.
//!
//! Exposes the commands and services so both the binary (`src/main.rs`) and the
//! integration tests (`tests/`) share one implementation.

pub mod commands;
pub mod services;
