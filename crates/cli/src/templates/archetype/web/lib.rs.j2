//! {{ name }} — Firefly Framework `web` (server-rendered) library.
//!
//! Exposes the page controllers and services so both the binary
//! (`src/main.rs`) and the integration tests (`tests/`) share one wiring.

pub mod controllers;
pub mod services;
