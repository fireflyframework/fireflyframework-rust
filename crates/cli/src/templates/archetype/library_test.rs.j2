//! Public-API tests for {{ name }}.

#[test]
fn greet_builds_a_greeting() {
    assert_eq!({{ package_name }}::greet("world"), "Hello, world!");
    assert_eq!({{ package_name }}::greet("firefly"), "Hello, firefly!");
}
